version: "3.8"

services:
  telegram-email-bot:
    image: dionlahh/codefetcherbot:latest
    container_name: telegram-email-bot
    restart: unless-stopped
    volumes:
      # Mount the configuration file
      - ./config.properties:/app/config.properties:ro
      # Mount logs to a persistent location
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - telegram-bot-network
    # Optional: Add resource limits
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.5"
        reservations:
          memory: 128M
          cpus: "0.25"
    # Optional: Add health check
    healthcheck:
      test: ["CMD", "python", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  telegram-bot-network:
    driver: bridge

volumes:
  bot-logs:
    driver: local
